* DONE test heteroscedastic
distribution of all residue is gaussian and for weighed residues? or just normalized by the  sqrt(y_i) value

** Residual Distribution Analysis (532 points from L1, L2, L4, 140220)
| Method             |   Std | Shapiro p | Conclusion            |
|--------------------+-------+-----------+-----------------------|
| lm_physics         | 2.807 |     0.000 | errors underestimated |
| outlier2_uniform   | 1.036 |     0.778 | ✓ BEST ~N(0,1)        |
| outlier2_shotnoise | 1.127 |     0.265 | ✓ Good, ~N(0,1)       |
| lm_reweighted      | 0.817 |     0.000 | errors overestimated  |

** Coverage Analysis (38 control wells with known pKa from L1+L2+L4+140220)
| Method               | K_err | Bias   | RMSE  | Coverage |
|----------------------+-------+--------+-------+----------|
| lm_robust            | 0.161 | +0.036 | 0.135 | 89.5% ✓  |
| lm_physics           | 0.119 | +0.059 | 0.162 | 84.2%    |
| odr_physics          | 0.097 | +0.011 | 0.132 | 81.6%    |
| pymc_physics_shared  | 0.109 | +0.060 | 0.171 | 81.6%    |
| outlier2_uniform     | 0.052 | +0.009 | 0.128 | 50.0% ✗  |
| outlier2_shotnoise   | 0.051 | +0.011 | 0.119 | 47.4% ✗  |

** Synthetic Data Validation (calibrated to match real data)
Parameters: n_points=7, buffer_sd=40, S0_y1=600, S0_y2=500
| Method           | Synth K_err | Real K_err | Synth Cov | Real Cov |
|------------------+-------------+------------+-----------+----------|
| lm_robust        |       0.140 |      0.161 |     89.0% |   89.5%  | ✓ Match
| lm_physics       |       0.121 |      0.119 |     89.0% |   84.2%  | ✓ Match
| odr_physics      |       0.122 |      0.097 |     86.0% |   81.6%  | ~Match
| outlier2_uniform |       0.112 |      0.052 |     87.0% |   50.0%  | Mismatch

** Key Finding: Heteroscedasticity Paradox
outlier2_uniform produces residuals closest to N(0,1) but has POOR coverage (50%)
because uncertainties are underestimated in absolute scale.
Synthetic data shows higher coverage (87%) → real data has additional systematic effects.

** Recommendation
- Use lm_robust (89.5% coverage) or odr_physics (81.6% coverage, lowest bias)
- Avoid residual-based methods for final uncertainty quantification
- Synthetic data generator calibrated to match real data (buffer_sd=40)

Plot: docs/residuals_all_methods.png
Results: comprehensive_fitting_results_with_L1.csv
* DONE Comprehensive comparison of fitting methods (2024-11-30)
benchmarks/compare_fitting_methods.py - Statistical comparison with synthetic & real data

** Generated Graphics (committed before function removal)
- benchmarks/synthetic_data_examples.png - Example synthetic data with/without outliers
- benchmarks/coverage_comparison.png - Coverage bar chart across methods
- benchmarks/bias_comparison.png - Bias bar chart across methods
- benchmarks/coverage_vs_bias.png - Coverage vs bias scatter
- benchmarks/uncertainty_calibration.png - K_err vs actual coverage
- benchmarks/bias_rmse_comparison.png - Bias vs RMSE scatter plots

** Final API (simplified after benchmarking)

*** fit_binding_glob(ds, robust=False, scale_covar=True)
- Default: standard least-squares with covariance scaling
- robust=True: Huber loss for outlier resistance (BEST bias with outliers)
- scale_covar=True: scales K_err by sqrt(reduced_chi_sq) when > 1

*** fit_binding_glob_robust(ds, error_model="uniform")
- Iterative error reweighting + Huber loss
- Higher coverage on real data but worse bias with outliers
- Use when error estimates are unreliable

** Results Summary
| Method | Clean Bias | Clean Cov | Outlier Bias | Outlier Cov |
|--------|------------|-----------|--------------|-------------|
| lm_standard | -0.002 | 94% | -0.059 | 100% |
| lm_robust | +0.024 | 62% | -0.018 | 80% |
| lm_robust+scale | +0.024 | 67% | -0.018 | 80% |
| fit_glob_robust | +0.041 | 82% | -0.097 | 84% |

** Final Benchmark Results (2024-11-30) - Realistic Synthetic Data
Synthetic data calibrated to match real data characteristics:
- y1: inverted curve, range ~460, 6% relative error (FRET acceptor)
- y2: normal curve, range ~900, 3% relative error (FRET donor)
- Outliers: 4σ drop in y1 at low pH (mimics real data pattern)

| Method | Clean Bias | Outlier Bias | Clean RMSE | Outlier RMSE |
|--------|------------|--------------|------------|--------------|
| outlier2 | -0.0001 | **0.007** | 0.048 | **0.049** |
| lm_standard | -0.003 | 0.032 | **0.041** | 0.053 |
| lm_robust | -0.002 | 0.010 | 0.049 | 0.052 |

Real data (38 wells):
| Method | Mean K | Std K |
|--------|--------|-------|
| outlier2 | 6.69 | 0.36 |
| lm_standard | 6.76 | 0.35 |
| lm_robust | 6.72 | **0.33** |

*** Conclusions:
1. outlier2 is BEST for outlier handling (4x lower bias than lm_standard)
2. lm_standard has best clean RMSE but worst outlier robustness
3. lm_robust is good compromise - second-best outlier bias, lowest variance on real data

** Outlier Magnitude Sensitivity Analysis (2024-12-01)
benchmarks/outlier_magnitude_comparison.png

Tested 2 outliers at low pH in y1 channel (DROP pattern like real data)
50 trials per outlier magnitude, scale_covar=True for all methods

| Sigma | Std Bias | Rob Bias | Std RMSE | Rob RMSE | Std Cov | Rob Cov |
|-------|----------|----------|----------|----------|---------|---------|
|     0 |   0.0053 |  -0.0006 |   0.0863 |   0.0933 |   96.0% |   94.3% |
|     3 |   0.0076 |   0.0162 |   0.0885 |   0.0933 |   98.0% |   97.2% |
|     5 |   0.0308 |   0.0215 |   0.0971 |   0.1051 |  100.0% |  100.0% |
|     8 |   0.1043 |   0.0437 |   0.1467 |   0.1337 |  100.0% |  100.0% |
|    10 |   0.1875 |   0.0665 |   0.2206 |   0.1440 |  100.0% |  100.0% |

*** Conclusions:
1. Robust (Huber) reduces bias by ~65% for severe outliers (10σ)
2. RMSE crossover at ~8σ - Robust wins for large outliers
3. Both maintain ~95-100% coverage thanks to scale_covar=True
4. For typical outliers (3-5σ): Standard WLS is sufficient
5. For severe outliers (>5σ): Use robust=True

** Residual Normality Analysis (2024-12-01)
benchmarks/residual_normality.png

Checks if standardized residuals follow Gaussian distribution (critical for valid CI):

Real Data (38 controls):
| Method | Shapiro p-value | Excess Kurtosis | Normality |
|--------|-----------------|-----------------|-----------|
| lm_standard | 0.003 | 1.2 | Rejected |
| lm_robust | 0.008 | 0.9 | Rejected |
| outlier2 | 0.015 | 0.8 | Rejected |

Synthetic with Outliers (N=100):
| Method | Shapiro p-value | Excess Kurtosis | Normality |
|--------|-----------------|-----------------|-----------|
| lm_standard | <0.001 | 2.5 | Rejected |
| lm_robust | 0.02 | 1.1 | Rejected |
| outlier2 | 0.08 | 0.5 | Marginal |

*** Key insight:
All methods show some deviation from normality on real data.
outlier2's Bayesian approach produces the most Gaussian residuals with outliers.

** Final Recommendation (2024-12-01)
- fit_binding_glob(ds, scale_covar=True) - DEFAULT for most use cases
  * Proper heteroscedastic error weighting naturally downweights outliers
  * When y1_err >> y2_err (typical: 100 vs 40), outliers in y1 have ~6x less influence
  * No information loss from outlier masking

- outlier2(ds, error_model="uniform") - When explicit outlier identification needed
  * Best residual normality with outliers (Shapiro p=0.08)
  * Most Gaussian residuals → most reliable uncertainty estimates
  * Use when downstream analysis requires Gaussian assumptions

** Action: REMOVE deprecated functions
- fit_binding_reweight - superseded by outlier2()
- fit_binding_glob_robust - standard WLS better with heteroscedastic errors
- fit_binding_glob_recursive_outlier - poor coverage, complex

** Keep (final API)
- fit_binding_glob(ds, scale_covar=True) - standard least-squares (PRIMARY)
- fit_binding_glob_recursive(ds) - conservative uncertainty estimates
- outlier2(ds, error_model="uniform") - Bayesian with explicit outlier detection

* for manuscript
   Key observations:

     - Coverage is similar (~85-88%) regardless of whether data was generated with physics or uniform errors
     - lm_physics, lm_robust, and odr_physics all achieve ~86-88% coverage
     - outlier2 methods achieve ~81-83% coverage (slightly lower but still reasonable)
     - lm_reweighted is consistently poor (~50% coverage) - overconfident
     - Coverage doesn't vary much with pKa - method performance is stable across the pKa range

** Synthetic Comparison: Data Error Model × Fit Error Model (N=30, pKa=6.0-8.0)
Results from benchmarks/compare_error_models.py

| Data Error | Fit Error | Method       | K_err | bias   | RMSE  | Coverage |
|------------+-----------+--------------+-------+--------+-------+----------|
| PHYSICS    | PHYSICS   | lm_physics   | 0.139 | +0.018 | 0.148 | 87.3%    |
|            |           | lm_robust    | 0.157 | +0.014 | 0.167 | 86.0%    |
|            |           | odr          | 0.149 | +0.007 | 0.178 | 86.0%    |
|            |           | outlier2     | 0.124 | +0.021 | 0.166 | 81.3%    |
| PHYSICS    | UNIFORM   | lm_robust    | 0.160 | +0.021 | 0.163 | 88.0%    |
|            |           | lm           | 0.140 | +0.020 | 0.148 | 88.0%    |
|            |           | odr          | 0.147 | +0.008 | 0.173 | 86.7%    |
|            |           | outlier2     | 0.124 | +0.023 | 0.167 | 81.3%    |
| UNIFORM    | PHYSICS   | lm_physics   | 0.139 | +0.017 | 0.149 | 88.0%    |
|            |           | lm_robust    | 0.158 | +0.023 | 0.162 | 86.7%    |
|            |           | odr          | 0.152 | +0.006 | 0.182 | 85.3%    |
|            |           | outlier2     | 0.124 | +0.020 | 0.167 | 81.3%    |
| UNIFORM    | UNIFORM   | lm           | 0.140 | +0.018 | 0.149 | 88.7%    |
|            |           | lm_robust    | 0.159 | +0.025 | 0.170 | 86.0%    |
|            |           | odr          | 0.149 | +0.007 | 0.177 | 86.0%    |
|            |           | outlier2     | 0.124 | +0.021 | 0.168 | 81.3%    |

*** Key findings:
1. Coverage is remarkably stable (~85-88%) across all error model combinations
2. Matching data and fit error models doesn't significantly improve coverage
3. outlier2 consistently achieves ~81% coverage regardless of error model
4. Best coverage (88.7%): Uniform data + Uniform fit + lm method
5. Real data's lower coverage for outlier2 (50%) suggests additional systematic
   effects not captured in synthetic simulation (reference pKa uncertainty, etc.)

* TODO Next Steps
+1. Analyze real experimental data characteristics
+1. Refine synthetic data generation to match real data
+1. Run comprehensive benchmarks
+1. Validate recommendations
+1. Implement cleanup based on validated results

* optimized
#+begin_src jupyter-python
  minimizer = Minimizer(
      _binding_1site_residuals, params, fcn_args=(ds,), scale_covar=True
  )

  # Use Huber loss for robustness to outliers
  huber_result = minimizer.minimize(method="least_squares", loss="huber")

  ...

      # Update weights based on residuals (IRLS step)
      if iteration < irls_iterations - 1:  # Don't update weights on last iteration
          start_idx = 0
          for da in current_ds.values():
              end_idx = start_idx + len(da.y)
              residuals = irls_result.residual[start_idx:end_idx]

              # Conservative weight update: inverse of residual magnitude with damping
              old_errors = da.y_errc[da.mask].copy()
              new_errors = np.maximum(
                  np.abs(residuals), 1e-6
              )  # Prevent division by zero

              # Apply damping to prevent instability
              damped_errors = (
                  1 - irls_damping
              ) * old_errors + irls_damping * new_errors
              da.y_errc[da.mask] = damped_errors

              start_idx = end_idx

    #+end_src

* weighting
prtecan builds each DataArray in Titration._create_data_array (src/clophfit/prtecan/prtecan.py) with y_errc = sqrt(signal + bg_err[label]^2) when buffer SEM exists (from Buffer
   fits/means), otherwise sqrt(signal) from the raw intensities. If no bg_err is provided, _create_ds calls weight_da (src/clophfit/fitting/core.py) to prefit each label with a 1-site model
   and replace y_err by the residual SEM (fallback = ones), while _create_global_ds calls weight_multi_ds_titration to do the same across labels and assigns any failed label a fallback error
   = (max successful y_err)×10. The downstream fitter fit_binding_glob uses residuals weighted by 1 / y_err, and outlier2 can optionally reweight uniformly or by shot-noise after a robust
   pass.

* DONE Final Unified Benchmark (2025)
benchmarks/final_comparison.py — N=100 repeats x 5 pKa (6.0-8.0) x 11 scenarios x 8 methods
Uses unified fit_binding_glob() API with physics error model (err = sqrt(signal + buffer_sd^2))

** Scenarios
Core (from prior run):
- 2label_heteroscedastic: y1 10X noisier (error_ratio=0.1), no outliers
- 2label_hetero_outlier: same + 1 low-pH 40% drop in y1
- 1label_clean: single label, no outliers
- 1label_outlier: single label + 40% low-pH drop
Extended:
- 2label_2outlier: 2 low-pH drops in y1 (error_ratio=0.1)
- 2label_eratio_0.5: moderate heteroscedasticity (y2 buffer_sd = 0.5 * y1)
- 2label_eratio_0.5_outlier: same + 1 low-pH drop
- 2label_equal_err: equal errors (error_ratio=1.0)
- 2label_equal_err_outlier: same + 1 low-pH drop
- 2label_uniform_weight: physics noise, but y_err stripped (uniform weights)
- 2label_sem_weight: physics noise, y_err set via SEM-based weight_multi_ds_titration

** Results Summary (averaged over K values)
| Scenario               | Method            | Cov95 | Cov90 |   Bias  |  RMSE  | SE/SD |
|------------------------+-------------------+-------+-------+---------+--------+-------|
| 2label_heteroscedastic | glob_huber        | 91.7% | 87.4% | +0.009  | 0.146  | 0.69x |
|                        | glob_ls           | 90.2% | 85.2% | +0.004  | 0.097  | 0.90x |
|                        | glob_irls         | 85.2% | 78.8% | +0.003  | 0.121  | 0.77x |
|                        | outlier2          | 84.4% | 80.2% | +0.005  | 0.100  | 0.81x |
|                        | odr_recursive     | 41.4% | 35.4% | +0.002  | 0.109  | 0.29x |
| 2label_hetero_outlier  | glob_huber        | 91.0% | 85.0% | -0.032  | 0.141  | 0.79x |
|                        | glob_ls           | 89.4% | 82.6% | -0.049  | 0.110  | 0.95x |
|                        | outlier2          | 84.8% | 75.8% | -0.043  | 0.113  | 0.82x |
|                        | glob_irls         | 77.6% | 69.8% | -0.095  | 0.156  | 0.80x |
|                        | odr_recursive     | 40.2% | 34.4% | -0.052  | 0.122  | 0.31x |
| 2label_2outlier        | glob_huber        | 91.8% | 87.7% | -0.005  | 0.139  | 0.93x |
|                        | glob_ls           | 89.2% | 82.2% | -0.003  | 0.101  | 1.11x |
|                        | outlier2          | 85.4% | 78.4% | -0.005  | 0.099  | 0.93x |
|                        | glob_irls         | 76.6% | 67.4% | -0.025  | 0.133  | 0.99x |
| 2label_eratio_0.5      | glob_huber        | 92.1% | 88.1% | +0.013  | 0.154  | 0.82x |
|                        | glob_ls           | 89.2% | 85.0% | +0.004  | 0.122  | 0.89x |
|                        | outlier2          | 86.4% | 80.4% | +0.003  | 0.126  | 0.79x |
| 2label_eratio_0.5+out  | glob_huber        | 88.8% | 82.7% | -0.064  | 0.162  | 0.88x |
|                        | glob_ls           | 86.8% | 79.4% | -0.075  | 0.145  | 0.93x |
| 2label_equal_err       | glob_huber        | 91.2% | 86.6% | +0.023  | 0.189  | 0.81x |
|                        | glob_ls           | 89.8% | 85.4% | +0.003  | 0.148  | 0.89x |
| 2label_equal_err+out   | glob_huber        | 86.2% | 80.2% | -0.077  | 0.226  | 0.73x |
|                        | glob_ls           | 86.0% | 78.4% | -0.109  | 0.186  | 0.91x |
| 2label_uniform_weight  | glob_irls         | 88.6% | 82.4% | +0.003  | 0.118  | 0.84x |
|                        | glob_ls           | 87.6% | 80.8% | +0.002  | 0.127  | 0.79x |
|                        | glob_huber        | 98.9% |       | +0.067  | 0.344  | BUG!  |
| 2label_sem_weight      | glob_huber        | 96.8% | 92.4% | +0.028  | 0.241  | 0.71x |
|                        | glob_irls         | 87.6% | 80.8% | +0.002  | 0.123  | 0.82x |
|                        | glob_ls           | 84.2% | 79.6% | +0.003  | 0.103  | 0.78x |
| 1label_clean           | glob_irls         | 89.2% | 84.2% | -0.007  | 0.169  | 0.93x |
|                        | glob_ls           | 88.2% | 84.2% | -0.008  | 0.168  | 0.92x |
|                        | glob_huber        | 88.7% |       |         |        | BUG!  |
| 1label_outlier         | glob_irls         | 80.0% | 73.6% | -0.156  | 0.239  | 0.96x |
|                        | glob_ls           | 78.6% | 71.8% | -0.162  | 0.243  | 0.96x |

** Methodological Findings
1. *glob_huber_outlier = glob_huber*: After Huber fit, no residuals exceed
   zscore threshold — outlier removal adds nothing on top of Huber loss.
2. *glob_irls = glob_iterative*: Both reweight branches execute the same
   code (set y_err = mean |residual|). Should differentiate or merge.
3. *Huber stderr is BROKEN* without physics-based y_err: produces absurd
   stderr (1e5-1e7) for 1-label data AND uniform/no-error data. The Huber
   loss in lmfit does not properly propagate errors when starting weights
   are uniform. With physics-based or SEM-based heteroscedastic y_err,
   Huber stderr is well-behaved (SE/SD = 0.69-0.93x).
4. *ODR consistently underestimates uncertainty*: SE/SD = 0.13-0.34x across
   all scenarios. Known issue: assumes orthogonal errors.
5. *Error ratio does NOT change method rankings*: glob_huber > glob_ls >
   outlier2 > IRLS/iterative > ODR holds at all error ratios (0.1, 0.5, 1.0).
6. *2 outliers*: glob_huber handles 2 outliers as well as 1 (91.8% cov).
   glob_ls drops slightly (89.2%) but remains robust. IRLS drops badly (76.6%).
7. *SEM-based weights help Huber* (96.8% cov!) but hurt glob_ls (84.2%).
   The scalar-per-label SEM assigns equal weight within a label, which
   oversmooths heteroscedastic per-point errors but properly captures the
   y1-vs-y2 ratio. Huber loss can exploit this to downweight outliers.
8. *Uniform weights*: glob_irls slightly beats glob_ls (88.6% vs 87.6%).
   Both are reasonable fallbacks when physics errors are unavailable.

** Key Conclusions
1. *Best 2-label with physics y_err*: glob_huber (91-92% cov, robust to 1-2
   outliers). The physics error model naturally handles heteroscedasticity;
   Huber loss adds outlier resistance on top.
2. *Best 1-label*: glob_ls (88-89% cov). Huber stderr is unreliable for
   1-label; glob_ls is safe and well-calibrated (SE/SD = 0.92x).
3. *Best fallback (no physics y_err)*: glob_ls or glob_irls (87-89% cov).
   Huber stderr is broken with uniform weights.
4. *IRLS/iterative always hurts*: reweighting amplifies outlier bias and
   drops coverage 5-15% vs glob_ls. Never the right choice.
5. *outlier2 outperformed by glob_ls*: deprecated, should be retired.
6. *No method reaches 95% CI*: best is 96.8% (Huber+SEM weights) but
   the standard scenario tops out at 91.8%. The 7-point titration with
   3+ params per label inherently limits precision.

** Pipeline Defaults (applied in commit f1968c6)
- *2-label global fit*: fit_binding_glob(ds, method="huber") — best for the
  real scenario (physics y_err + possible low-pH outliers in y1).
- *1-label single fit*: fit_binding_glob(ds) — standard WLS, Huber unsafe.
- *Fallback (no bg_err)*: weight_multi_ds_titration() then glob_ls — safe path.

* DONE Real-data comparison: L2 and L4 plates (2026)
scripts/compare_methods.sh + scripts/compare_methods.py
Sampler: default (pytensor/CPU), 2000 draw samples, 4 chains.

** Fit-method results (lb0–lb3)
Plates L1–L4 measured. L1 uses fixed x_err=0.1 (manual pH record, not SD);
L2–L4 use measured addition SD from multiple wells. n=wells fitted.

*** lb0 (y1 only — noisy, 10× higher uncertainty, frequent low-pH outliers)
| Method      |  L1 std (n=90) |  L2 std (n=~96) |  L3 std (n=92) |  L4 std (n=~96) |
|-------------|----------------|-----------------|----------------|-----------------|
| lm          |          2.441 |           1.119 |          0.287 |           0.978 |
| irls        |          2.407 |           1.092 |          0.286 |           0.965 |
| huber       |          2.432 |           1.010 |          0.287 |           0.913 |
| outlier_2.5 |          2.432 |           1.010 |          0.287 |           0.913 |
| outlier_2.0 |          2.308 |           1.005 |          0.287 |           0.894 |

*** lb1 (y2 only — cleaner label)
| Method      |  L1 std |  L3 std |
|-------------|---------|---------|
| lm          |   0.779 |   0.234 |
| irls        |   0.654 |   0.241 |
| huber       |   0.779 |   0.234 |
| outlier_2.5 |   0.779 |   0.234 |
| outlier_2.0 |   0.775 |   0.235 |

*** lb2 (global, both labels — production path)
| Method      |  L1 std |  L2 std |  L3 std |  L4 std |
|-------------|---------|---------|---------|---------|
| lm          |   0.619 |   0.291 |   0.227 |   0.379 |
| irls        |   0.562 |   0.288 |   0.233 |   0.388 |
| huber       |   0.619 |   0.281 |   0.227 |   0.378 |
| outlier_2.5 |   0.619 |   0.281 |   0.227 |   0.378 |
| outlier_2.0 |   0.610 |   0.281 |   0.227 |   0.380 |

** Key fit-method findings
1. *huber consistently best for L2/L4* (calibrated x_err): 10% std reduction
   vs lm on noisy lb0; equal or slightly better than lm on lb2.
2. *irls is unexpectedly best for L1* (fixed x_err=0.1 — poorly calibrated):
   9–16% improvement over lm/huber on lb1 and lb2. When the initial y_err is
   uniform/poorly calibrated, IRLS adaptive reweighting outperforms Huber's
   heavy-tail protection. L3 shows no benefit (well-calibrated plate).
   → The optimal method depends on y_err quality, not just noise magnitude.
3. *irls/wls/iterative produce identical outputs* — all three implement the
   same IRLS algorithm. → Retire wls and iterative; keep irls as documented alias.
4. *Outlier removal at zscore 2.5 is identical to plain huber* — no real
   outliers detected at that threshold in any plate. zscore 2.0 gives marginal
   improvement on noisy lb0 for L1/L2/L4 but not L3 (~1–5% reduction).
5. *L3 is the cleanest plate*: all methods essentially identical (std≈0.227–0.233);
   the choice of method barely matters when y_err is well-calibrated.
6. Recommendation: use huber as default for calibrated plates (L2/L3/L4);
   consider irls when x_err is fixed/poorly-calibrated (L1-type plates).
   Offer outlier at zscore 2.0 as optional mode for very noisy lb0 data.

** MCMC-mode results (lb4, huber seeding)

| Mode            |  L2 std | L2 div |  L4 std | L4 div |
|-----------------|---------|--------|---------|--------|
| single          |   0.289 |   47+  |   0.381 |   44+  |
| multi           | *0.245* |      0 |   0.364 |      0 |
| multi-noise     |   0.277 |      0 | *0.355* |      0 |
| multi-noise-xrw |   0.293 |      0 |   0.381 |      0 |
| huber-lb2       |   0.281 |    n/a |   0.378 |    n/a |

** Key MCMC findings
1. *single mode is deprecated*: 44-47+ divergences per plate — posterior
   geometry is poor (banana-shaped) with only 7-13 pH points per well.
   Hierarchical models fix the geometry completely (0 divergences).
2. *multi is best for L2* (std 0.245, −15% vs huber-lb2): tight pH calibration
   means the shared-pH-per-step model is appropriate.
3. *multi-noise is best for L4* (std 0.355, −6% vs huber-lb2): the per-step
   noise term accommodates slightly more pH variability between wells.
4. *multi-noise-xrw does NOT help*: for L2 it is WORSE than single (std 0.293
   vs 0.289); for L4 it equals single (both 0.381). The per-well pH random
   walk adds unnecessary uncertainty when pH is well-calibrated by additions.
   xrw may be appropriate only when individual well pH was NOT measured.
5. *Sampler viability*: only the default pytensor/CPU sampler is reliable for
   all modes. nutpie hangs on hierarchical (multi+), numpyro is broken (JAX
   version mismatch), blackjax works for single only (CPU fallback after OOM).

** Bugs fixed during this comparison
- fix(bayes): weighted_stats called with mismatched lengths in
  fit_binding_pymc_multi_noise_xrw — values included all wells with r.result
  but stderr_ additionally excluded None-stderr wells, causing
  "zip() argument 2 is shorter than argument 1" for H04 (OVER values). Fixed
  in commit b542fa8.
- fix(scripts): SIGPIPE false-positive in sampler benchmark — head -5 in a
  pipefail shell caused ppr to appear failed. Fixed via temp file. (commit 68583c2)

** Recommended defaults
- Deterministic: fit_binding_glob(ds, method="huber")  (lb2, both labels)
- MCMC: multi or multi-noise (plate-dependent; multi when pH well-calibrated)
- Retire: irls, wls, iterative (redundant); single (divergences); multi-noise-xrw

* TODO Heuristic y_err correction via proportional noise α
** Background
The current heuristic y_err = sqrt(signal + bg_err²) captures only readout
noise and Poisson noise. The true variance model revealed by multi-noise MCMC is:
  Var(y) = σ_read² + gain·μ + α²·μ²
where the *proportional* term α²·μ² is DOMINANT at typical fluorescence signal
levels (200-1000 RFU).  This means high-signal y1 wells are systematically
under-weighted (treated as more reliable than they really are).

** Can α be estimated from input data statistics?
Three approaches were evaluated, all failing for different reasons:
1. CV of replicate wells: dominated by biological/concentration variance, not
   instrument noise.  Estimate: ~0.45 vs MCMC ~0.06 — completely wrong.
2. Median(|residual|/signal) from single-well fits: S0/S1 absorb proportional
   noise into plateau parameters.  Result: 0.43 vs MCMC 0.057 for L2 y1.
3. OLS regression Var(r) ~ σ²+gain·s+α²·s²: works for high-SNR labels where
   α dominates (L4 y1: 0.104 vs MCMC 0.102; L4 y2: 0.033 vs MCMC 0.046)
   but fails when σ_read is large (L2 y1: α→0 from regression vs MCMC 0.057).
   Only 7 pH steps + free S0/S1 make the 3-parameter decomposition ill-conditioned.

*Conclusion*: α cannot be reliably estimated from raw data statistics.
MCMC multi-noise is the only reliable estimator. α values from MCMC should
be stored and reused as empirical priors for the heuristic y_err correction.

** True noise per plate (from multi-noise MCMC)
| Plate | α_y1  | α_y2  | σ_read_y1 | σ_read_y2 | gain_y1 | gain_y2 | α_y1/α_y2 | MCMC quality     |
|-------|-------|-------|-----------|-----------|---------|---------|-----------|------------------|
| L2    | 0.057 | 0.006 |      18.2 |      19.1 |    1.53 |    0.03 |       9.5 | BAD (bg contam)  |
| L3    | 0.042 | 0.019 |       2.9 |       1.1 |    0.06 |    0.14 |       2.2 | GOOD             |
| L4    | 0.102 | 0.046 |       2.7 |       1.1 |    0.28 |    0.11 |       2.2 | GOOD             |
| L1    | 0.020 | 0.026 |      22.3 |       6.3 |    0.61 |    0.02 |       0.8 | FAIL (4433 divg) |

Key observations:
- α_y1 varies across plates but L1 is anomalously low (0.020) and may be unreliable.
- L1 MCMC completed (6054 s) but with 4433 divergences and low ESS — ALL L1 noise
  parameters are unreliable. High gain=94 + very high buffer background create
  ill-conditioned geometry.
- L2 and L1 have anomalously high σ_read — traced to specific buffer well contamination
  (see "Buffer well data quality" section below).
- L3 and L4: MCMC is trustworthy; σ_read_y1≈2.7-2.9, σ_read_y2≈1.1; gain small.
- L3 and L4 α_y1/α_y2 ratio identical (2.2) despite different concentrations →
  proportional noise is label-independent for these plates (same sample prep quality).
- L2 α_y1/α_y2 = 9.5 is suspicious and may reflect the buffer contamination affecting
  α estimation indirectly.

** Implementation (DONE)
1. --noise-alpha CLI option added (one value per label, in sorted order).
2. TitrationConfig.noise_alpha: tuple[float, ...] field added.
3. _create_data_array: y_err² = signal + bg_err² + (α·signal)².
4. Validated: std_K unchanged; mean_sK halved (biology dominates, not noise).

* TODO Instrument metadata and concentration effects
** Gain settings (from metadata-labels.txt)
| Plate | y1 gain | y2 gain | y1 median signal | y2 median signal | concentration |
|-------|---------|---------|-----------------|-----------------|---------------|
| L1    |      94 |  (auto) |          75 RFU |         180 RFU | LOW (~15× less) |
| L2    |      79 |      55 |        1285 RFU |         682 RFU | high          |
| L3    |      77 |      55 |         777 RFU |         546 RFU | medium        |
| L4    |      76 |      53 |         645 RFU |         376 RFU | medium-low    |

Both labels: Ex=400nm for y1 (ECFP-like), Ex=485nm for y2 (EYFP-like);
Em=535nm for both. Integration 20µs, 10 flashes averaged.

Key observations:
- L1 y1 signal is ~15-17× lower than L2-L4 — the higher PMT gain (94) partially
  compensates but leaves the median at 75 RFU (shot-noise dominated).
- L1 y2 reaches negative values after background subtraction (−269 minimum)
  → readout noise is significant at this concentration.
- L2-L4 gain settings span only 76-79 (y1) and 53-55 (y2): nearly identical,
  yet signal levels differ 2× → protein concentration is the primary driver.
- No gain field for L1 y2 → likely auto-gain or single-label optimization.

** Connection to MCMC noise parameters and buffer well data quality
The Tecan "Gain" (PMT setting) is NOT directly the MCMC "gain" (Poisson
scaling factor). The MCMC α (proportional noise) does not correlate with
instrument gain or signal level:
| Plate | y1 gain | y1 signal | α_y1  | σ_read_y1 |
|-------|---------|-----------|-------|-----------|
| L2    |      79 |      1285 | 0.057 |      18.2 |
| L3    |      77 |       777 | 0.042 |       2.9 |
| L4    |      76 |       645 | 0.102 |       2.7 |

α_y1 ranking: L4 > L2 > L3 — opposite of signal level. This suggests
α reflects sample-preparation quality (within-well pipetting consistency)
rather than detector physics.

*** Buffer well y1 drift: pH-sensitive autofluorescence (NOT evaporation)
Experiment duration is ~30 min; both labels acquired simultaneously →
evaporation is negligible. The drift in y1 buffer signals (~3000-4000 RFU)
across pH steps reflects pH-sensitive autofluorescence of buffer components:
small acid aliquots are added to buffer wells too (to maintain consistency),
causing real pH changes → the buffer's intrinsic fluorescence at 400nm
excitation responds to pH. This is correctly removed by the background
subtraction at each pH step. The residual is NOT readout noise.

y2 buffer wells (near noise floor, 30-90 RFU) are useful for σ_read estimation:
| Plate | Clean buffer wells (y2) | σ_read_y2 from buffer | MCMC σ_read_y2 |
|-------|------------------------|----------------------|----------------|
| L2    | C12,D12,E12,F01        |               ~4.2   |           19.1 |
| L3    | D01,D12,E01,E12        |               ~1.6   |            1.1 |
| L4    | D12,E01,E12 (D01 excl) |               ~0.8   |            1.1 |

For L3 and L4, buffer-derived σ_read_y2 matches MCMC within a factor of 2.

*** Anomalous buffer wells (data quality issues)
*L2 y2*: D01=146 RFU and E01=97 RFU at pH=6.563 (expected ~70 RFU both).
Transient contamination at that pH step (bubble? micro-spill?). Background
inflated by ~20 RFU at that step → all protein well y2 signals over-corrected
by ~20 RFU at pH=6.56. This systematic residual cannot be fit by the model
→ MCMC compensates with anomalously high σ_read_y2=19.1.
Clean buffer (C12,D12,E12,F01) gives σ_read_y2≈4.2 RFU — more realistic.

*L4 y2*: D01 signals decrease 170→137→94→63→48→37 RFU (pH 8.32→4.97) —
a clear sigmoidal pH-sensitive response. This well contains fluorescent
protein (likely mislabeled or cross-contaminated). Because D01 is included
in the background mean, protein well y2 signals are over-corrected at high pH.
At pH=8.32: bg_mean=70.2 (sd=57.6) vs expected 37 RFU → bg_err_mean=28.8 RFU!
After excluding D01: bg_err_mean drops to 1.4 RFU. MCMC σ_read_y2=1.1 is
reasonable and is not affected because the model fits protein signals post-bg.

*L1 y1+y2*: Buffer signals are 16,000-29,000 RFU (y1) and 6,000-15,000 RFU
(y2) due to very high PMT gain (94). Buffer wells show highly nonlinear
pH-dependent fluorescence. After linear detrending, residual SD≈700 RFU (y1)
and ≈350 RFU (y2) — dominated by the nonlinear background shape, not readout
noise. The MCMC failed with 4433 divergences; L1 noise parameters unreliable.

* TODO Per-well pH variation and CTR K spread
** Background
The `multi` MCMC model assumes all wells receive the same pH at each
addition step (shared x_diff per step). The question is whether this
assumption is valid or whether per-well pH adjustment is necessary.

** Measured pH SD per step (reference wells, list_x_true.csv)
pH is measured in 3-4 reference wells per addition step (mean ± SD).
| Plate | midpoint-range pH SD (RMS) |
|-------|--------------------------|
| L2    | 0.018 ± 0.002 pH units   |
| L3    | 0.018 ± 0.003 pH units   |
| L4    | 0.021 ± 0.008 pH units   |

MCMC x_diff posteriors are TIGHTER than measured SD — fluorescence data
constrains pH more than 3-4 reference measurements alone.

** CTR K spread (L3 cleanest plate)
CTR wells contain the same protein → K spread reflects fitting precision.

| CTR protein | n | std_K | mean_sK |
|-------------|---|-------|---------|
| E2GFP       | 4 | 0.113 |   0.064 |
| V224Q       | 4 | 0.080 |   0.030 |
| S202N       | 4 | 0.063 |   0.053 |

** Key finding: fitting precision limit with 7 pH points
The residual K spread in CTR wells (σ ≈ 0.07–0.09) is primarily due to
the fundamental fitting uncertainty with only 7 pH data points and 3
free parameters (K, S0, S1). The lmfit-reported sK understates the true
per-well K uncertainty when the noise model is imperfect (chi²/dof >> 1);
the true per-well K precision is ~0.07 pH.

This is NOT primarily a pH spatial gradient — the measured pH SD per step
(0.018) accounts for only ~0.018² / 0.08² ≈ 5% of the K variance.
The dominant source is the Fisher-information limit with 7 data points.

** Measured pH SD constrains what MCMC can recover
The x_diff posterior SD ≈ measured pH SD at most steps, confirming MCMC
recovers the pH uncertainty correctly.

** multi_noise_xrw: physically correct per-well pH model
The `multi_noise_xrw` model is the correct physical description of the
titration:
- Shares global x_true pH values per step (same plate, same buffer)
- Models per-well cumulative deviations via a random walk:
    z_pip[t, w] ~ Normal(0, 1)   for each step t>0 and well w
    x_dev[:,w] = concat([0, cumsum(σ_pip · z_pip[:,w])])
    x_per_well = x_nominal[:,None] + x_dev
- σ_pip (default 0.02 pH units) = per-acid-addition pipetting precision
- Cumulative: if at step i too much acid was added to well w, ALL steps
  j>=i are shifted by the same direction. This matches the physics.

Previous assessment that xrw "was worse" is likely WRONG — it reflects
computational convergence issues, not model quality. The nutpie sampler
failed to complete after 18 hours for multi_noise_xrw (blackjax/numpyro
should be tried instead for JAX-based NUTS over ~80 wells × 6 steps =
480 per-well pH parameters). The model is over-parameterized but physically
motivated.

** Optimal approach (updated)
Use `multi_noise_xrw` for the primary analysis when computational resources
allow (blackjax or numpyro recommended). For fast exploration, `multi_noise`
with shared pH per step is a valid approximation given that pH SD (~0.018 per
step) is small relative to the K precision limit (~0.07 pH).

Before rerunning: exclude anomalous buffer wells (L2 D01/E01 at pH=6.56;
L4 D01 y2 protein contamination) from background estimation.

1. 1 label s0 s1 fissi no drop
2. 1 lab
